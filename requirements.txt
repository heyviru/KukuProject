# Core Dependencies
openai==1.54.0
faster-whisper==1.0.3
pydub==0.25.1
flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0

# Production Server
gunicorn==21.2.0

# Audio Processing (system dependency: ffmpeg required)
# Install ffmpeg: brew install ffmpeg (macOS) or apt-get install ffmpeg (Linux)

# Optional: Testing
pytest==7.4.3
pytest-flask==1.3.0